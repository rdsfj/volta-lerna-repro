{
  "name": "main",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "clean": "rm -f packages/*/src/main.{js,d.ts}"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.1.4"
  },
  "volta": {
    "node": "18.6.0",
    "npm": "9.6.7"
  }
}
