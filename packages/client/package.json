{
  "name": "client",
  "version": "1.0.0",
  "scripts": {
    "dev": "echo package client started in $(pwd) && tsc --watch src/main.ts",
    "build": "tsc src/main.ts"
  },
  "type": "module",
  "devDependencies": {
    "typescript": "^5.1.6",
    "universal": "^1.0.0"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
